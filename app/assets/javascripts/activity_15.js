(function(angular, app) {
    "use strict";
    app.controller("Activity15Controller",["$scope",  function($scope) {
	$scope.apple 
	$scope.validate = function(){
	    if ($scope.apple != "an apple")
	    {
		alert("an apple")
	    }
	    if ($scope.hen != "a hen")
		{
		    alert("a hen")
		}
	    if ($scope.oven != "an oven")
	    {
		alert("an oven")
	    }
	    if ($scope.cake != "a cake")
		{
		    alert("a cake")
		}
	    
	    else {alert ("All are correct! Good")}
	}
    }]);
})(angular, myApp);	

